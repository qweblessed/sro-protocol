export type Token = {
    decimals: string;
    id: string;
    name: string;
    symbol: string;
};

export type TokensResponse = {
    token0:{
      decimals:string,
      id:string,
      name:string,
      symbol:string
    }
  }
  

